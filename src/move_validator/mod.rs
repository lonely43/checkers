mod moves;
mod rules;

use crate::board::Board;

#[allow(dead_code, unused)]
pub fn is_valid_move(board: &Board, from: (usize, usize), to: (usize, usize)) -> bool{
   true // temporarily
}
#[allow(dead_code, unused)]
pub fn get_all_valid_moves(board: &Board, player: i8) -> () { // -> Vec<(i8,i8)>
    // logic
}
